# Basic Math Interpreter

---

This is basically a language but not really. This is me practicing creating a basic interpreter that has a lexer, that
passes tokens to a parser, that passes instructions to the interpreter to execute.